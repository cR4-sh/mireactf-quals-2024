q = ?
p = ?

N = p*q
e = 23
flag = int.from_bytes(b"????????????????????????????????????????")

def crypt(text: bytes, e: int, n: int) -> bytes:
    encrypted_int = [pow(i, e, n) for i in text]

    return encrypted_int


print(N)
print(e)
c = crypt(flag, e, N)
print(c)